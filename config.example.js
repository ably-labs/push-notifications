// Configuration here will preload in the demo.
// ablyAppName is for reference only
// apiKey should be correctly configured in Ably Dashboard with correct capabilities will require Push Admin for demo purposes.
// channel rule should have push notifications enabled
window.ABLY_CONFIG = {
    ablyAppName: '',
    apiKey: '',
    channel: '',
};

